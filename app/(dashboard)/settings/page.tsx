/**
 * app/(dashboard)/settings/page.tsx
 *
 * Route alias mapping /settings to the main Structa Admin & Settings page.
 */
import AdminPage from "../admin/page";

export default function SettingsPage() {
  return <AdminPage />;
}
